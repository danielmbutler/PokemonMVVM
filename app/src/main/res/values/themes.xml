<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PokemonApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/pokemon_dark_red</item>
        <!-- Customize your theme here. -->

        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:editTextColor">@color/white</item>


        <item name="fontFamily">@font/pokemon_card_font</item>
    </style>

    <!-- Custom SearchBar theme for List Fragment -->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/ic_baseline_search_24</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@color/pokemon_dark_red</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">@string/search_hint</item>


    </style>

    <!-- Custom splash theme -->
    <style name="splashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:statusBarColor">@color/pokemon_yellow</item>
    </style>

    <!--- Dialog Theme -->
    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/pokemon_card_color_dark</item>
        <item name="android:buttonBarButtonStyle">@style/MyButtonsStyle</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>
    <style name="MyButtonsStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">

    </style>

    <string name="search_hint">Search for Pokemon by name..</string>
</resources>